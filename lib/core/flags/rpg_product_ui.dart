/// Product UI for the standalone RPG package system is sealed.
///
/// The engine, importer, editor route, and tests remain. Character cards do
/// not carry this content, so the feature is hidden from Settings and Chat.
const bool kRpgProductUiEnabled = false;
