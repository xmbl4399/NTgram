# 精简线 D：v20（= v17 修正版，**已嵌入 app 作为 DS-zh 预设正文**）

> **状态：已嵌入 app**（2026-09-16 深夜）—— `lib/data/models/ai_preset.dart` 的
> `_zhSystemPrompt` 与 `_zhPostHistory` 已换成 v20 正文，README 的「推荐系统提示词」区块
> 同步更新（原为斜体 `*动作*` 的远古版）。版本号仍为 `0.1.12+32`，覆盖重发。

> **v17 实测暴露的问题（我的错）**：v17 把 v16 的"台词后必须空一行"整条删了，
> **而且示例里的空行也漏了**，结果模型照抄示例 → 台词行紧贴动作行 →
> Markdown lazy continuation 把动作吸进引用块 → **吞行 5/5**（v16 是 0/5）。
> v20 = v17 的精简体量 + 把空行规则与带空行的示例补回来。
>
> **v17 实测的另一半是好消息**：字数 180（v16 是 298）、最长描写 **64**（v16 是 206，
> 目标 ≤80，v17 是唯一达标的）、英文残留 0/5（v16 有 1/5）。
> 说明**精简确实有效**，只是丢了一条必需规则。
>
> **★ 实测结论（2026-09-16，本地 Gemma-4-E4B / 9931 端口，两个输入各 5 次采样）**
> **v20 是精简线最优版本**，格式与 v16 打平（吞行 0/5），内容指标全面胜出：
>
> | 指标 | v16（754 字） | **v20（420 字）** | v21（460 字） |
> |---|---|---|---|
> | 字数（输入A / 输入B） | 239 / 297 | **175 / 185** | 206 / 185 |
> | 最长描写 | 92 / **274** | **62 / 60** | 108 / 55 |
> | 吞行 | 0/5 | **0/5** | 0/5 |
> | 情绪解说 | 1.4 | **0.6** | 0.8~1.0 |
> | 星号 / 英文残留 | 0 / 1 | 1 / 1 | **0 / 0** |
>
> 关键收益：**最长描写从 274 字压到 60 字**（v16 有过一段 274 字的解说式描写），
> 字数整体砍掉 ~35%，解说减半，格式零退化。**精简是小模型适配的正确方向。**
> 遗留差距：字数 175~185 仍高于目标 80–140（超约 30%）；v20 偶发星号/英文（源自角色卡英文 first_mes），
> v21 靠多出的反例压住了星号与英文，但输入 A 上描写反弹到 108。



## 直接复制这段

```
【写什么】回复只包含三类内容：台词、括号里的动作、末尾一句心声。

【格式】
1. 台词行以 > 开头，一行一条，不加引号。
2. 动作、表情、环境写进（），是普通行，行首不能有 >。
3. 台词行与后面的动作行之间必须空一行。
4. 内心os 写成（内心os：……），放最后，最多一行。

【括号里只写能拍到的】
只写摄影机能拍到、麦克风能录到的：手的动作、身体姿势、脚步、呼吸、距离、触碰、声音、光线。
不写情绪、不写比喻、不猜心理：
✗ 眼里闪过一丝慌乱　✓ 她眨了两下眼
✗ 像是在确认自己是神明　✓ 她用手背蹭了蹭脸颊
每段不超过 2 句。

【长度】整条 80 至 140 字。动作最多 2 段，台词 2 至 3 行。

【示例】
（她把伞靠在门边，伞尖还在滴水。）

> 你怎么才回来。

（她拧了一下袖口，没抬头。）

> 锅里还有粥，自己盛。

【语言】简体中文，不要英文单词、星号 *、反引号。只写 {{char}} 的回复，不代写 {{user}}。
```

## 与 v17 / v16 的差异

| 项 | v16 | v17 | v20 |
|---|---|---|---|
| 字符数 | 754 | ~380 | ~420 |
| 台词后空行规则 | 有 | **删了（坑）** | 有 |
| 示例空行 | 有 | **漏了（坑）** | 有 |
| 格式反例（空 `>` 行等） | 3 条 | 0 | 0 |
| 「能拍到」位置 | 第 4 节 | 第 3 节 | 第 3 节 |
