# v23：ST 写法精简 A —— 删掉 ✗/✓ 反例，规则合行【★ 实测最优，建议作为 DS-zh】

> **★ 实测结论（2026-09-17，llama.cpp / **Qwen3.5-4B** / 9931，两个输入各 5 次采样）**
> **精简成立，v23 是本轮最优版本**：402 字符（v22 是 546，**−26%**），格式与内容指标全面不劣于 v22：
>
> | 指标 | v22 (546) | **v23 (402)** | v24 (318) | v25 (249, 无示例) |
> |---|---|---|---|---|
> | 字数 A / B | 234 / 232 | **194 / 226** | 177 / 188 | 295 / — |
> | 最长描写 A / B | 88 / 43 | **79 / 139** | 47 / 140 | **294** / — |
> | 吞行 | **2/5** / 0/5 | **0/5 / 0/5** | 0/5 / 0/5 | 0/5 / — |
> | 情绪解说 A / B | 1.2 / 0.8 | **0.4 / 0.4** | 1.2 / 0.2 | 0.6 / — |
> | 英文残留 A / B | 0/5 / 0/5 | 1/5 / 2/5 | 0/5 / 1/5 | **4/5** / — |
>
> **三条硬结论**
> 1. **示例块删不得**：v25（只删示例，其余同 v24）立刻烂 —— 英文残留 **4/5**、最长描写 **294** 字。
>    示例才是小模型真正在抄的"规范本身"。
> 2. **✗/✓ 反例删得**：v22 里的 3 组反例既没压住故障（吞行 2/5），字数还多 40%。删掉后 v23 全面更好 ——
>    与 v21 的结论一致：**反例加多了反而分散小模型注意力**。
> 3. **不能再往下砍**：v24（318 字符）输入 A 上解说反弹到 1.2、裸星号 2/5 —— 规则合并到极限就守不住内容纪律。
>
> **未解决的残留（三版都有）**：**3/5 采样把动作写成 `> *动作*`**（动作行被加上 `>`，
> 整条被并进同一个引用块）。v22/v23/v24/v26 都写了"行首不能有 >"或加了反例，**都压不住**（v26 反例版更差：解说 1.2、英文 3/5）。渲染层有兜底，视觉上不致命，但格式不纯。

> **已知语义瑕疵（2026-09-17 复查，实测确认不影响生产，故保留原样）**
> 【只写能拍到的】与【格式】确实冲突，共 3 处：
> 1. 【格式】2 授权写「**旁白**」，而本节的「不写比喻」禁止它 —— 旁白在 ST 语境里天然容纳比喻与氛围；**这是硬冲突**。
> 2. 【格式】4 要求写「内心」，本节的「不写心理」若被读成全局约束，内心条自相矛盾 —— **实测已见后果**：
>    本地 4B 在 seed 4242 那次把 `*斜体动作*` 原样复制进 `（）` 充数（因为不知道该往括号里写什么）。
> 3. 本节标题「只写能拍到的」没有范围前缀，读起来像全局约束；真正的范围是正文里的「**斜体里**」三个字。
>
> **溯源**：v16/v20 动作是 `（）`，标题写的是「**括号里**只写能拍到的」（有范围前缀），内心带 `内心os：` 前缀自然豁免 → **无冲突**。
> v22 改 ST 语法时标题改为「斜体里只写能拍到的」（正确）；v23 精简时把「斜体里」从标题挪进正文，
> 标题只剩「只写能拍到的」→ **范围前缀丢失，冲突由此产生**。「旁白」也是 v22 新增、v16/v20 没有的词。
>
> **为何不改**：DeepSeek 上 v23 / v27 / v28 / v29 四版**全部 0 违规、字数全达标** —— 大模型读得懂上下文，
> 冲突不产生症状；本地 4B 上三个「消冲突」版也**没有一致优势**（v28/v29 最长描写反弹到 135/161）。
> 改动有回归风险、收益为 0，故 v23 原样保留。
> **日后若要清理语义，用 v27**（删「旁白」+ 把「每段不超过 2 句」移入【长度】）—— 改动最小，且 DeepSeek 上同样满分（143/147 字）。

> 原始说明（基于 v22，只做减法）：
> 去掉「斜体里只写能拍到的」下面的 3 组 ✗/✓ 反例（v22 里占 ~60 字），
> 其余规则合并同类行。**示例块与空行规则原样保留**（v17 的教训：示例是规范本身）。

## 直接复制这段

```
【写什么】回复只包含：台词、*斜体*动作、末尾一句内心。

【格式】
1. 台词行：> "台词"，一条一行。
2. 动作、神态、环境、旁白：整段用 * 包住，普通行，行首不能有 >。
3. 台词行与上下其他行之间必须空一行，否则动作会被并进同一个引用块。
4. 内心：用（）包住，放最后，最多一行。

【只写能拍到的】斜体里只写摄影机能拍到、麦克风能录到的：动作、姿势、脚步、呼吸、距离、触碰、声音、光线。不写情绪、比喻、心理。每段不超过 2 句。

【长度】整条 80 至 140 字。动作最多 2 段，台词 2 至 3 行。

【示例】
*她把伞靠在门边，伞尖还在滴水。*

> "你怎么才回来。"

*她拧了一下袖口，没抬头。*

> "锅里还有粥，自己盛。"

【语言】简体中文，不要英文单词、反引号。只写 {{char}} 的回复，不代写 {{user}}。主动推动剧情，避免与上一条重复用词。
```
