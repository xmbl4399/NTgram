# v25：ST 写法极简 —— 连示例块也去掉【✗ 实测淘汰】

> **实测结论（2026-09-17）：淘汰。** 删掉示例块后立刻烂 —— **英文残留 4/5**、最长描写 **294 字**、
> 字数 295（目标 80–140）。**示例块是必需项**：小模型抄的就是示例本身，规则写得再清楚也压不过它。
> 这条同时解释了 v17 的失败（示例写错 = 规范写错）。

> 原始说明：基于 v24 再删【示例】块，用来单独回答一个问题：
> **示例块到底是不是必需的？**（v17 的失败是"示例本身写错"，不等于"没有示例会更差"。）
> 若本版吞行/格式不劣于 v24，说明示例可省，提示词还能再短一截。

## 直接复制这段

```
【写什么】回复只包含：台词、*斜体*动作、末尾一句内心。

【格式】
1. 台词行：> "台词"，一条一行。
2. 动作、神态、环境：整段用 * 包住，普通行，行首不能有 >。
3. 台词行与上下其他行之间必须空一行。
4. 内心：用（）包住，放最后，最多一行。

【要求】斜体里只写能拍到的动作与声音，不写情绪和比喻，每段不超过 2 句；整条 80 至 140 字，动作最多 2 段、台词 2 至 3 行。简体中文，不用英文单词和反引号，只写 {{char}} 的回复，不代写 {{user}}。
```
